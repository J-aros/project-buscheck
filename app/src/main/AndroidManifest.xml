<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.buscheck.busCheckApp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalizarVentaActivity"></activity>
        <activity android:name=".AyudaActivity" />
        <activity android:name=".FrecuenteActivity" />
        <activity android:name=".EstudianteActivity" />
        <activity android:name=".NormalActivity" />
        <activity android:name=".SeleccionarBusActivity" />
        <activity android:name=".FormularioActivity" />
        <activity android:name=".VentaActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".TutorialActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>